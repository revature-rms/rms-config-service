version: '3.4'

services:
  rms-config-service:
    image: rms-config-service
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      JAVA_OPTS: -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005,quiet=y
    ports:
      - 8888
      - 5005:5005